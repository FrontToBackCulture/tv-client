// src/shell/ModeTabs.tsx
// Mode tabs are currently hidden — everyone runs in `all` mode and module
// visibility is controlled by team/workspace settings. Component kept as a
// stub for easy re-enable.

export function ModeTabs() {
  return null;
}
