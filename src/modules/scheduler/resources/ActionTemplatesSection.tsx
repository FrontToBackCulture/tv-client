// Action templates — reusable action node configurations

export function ActionTemplatesSection() {
  return (
    <div className="px-6 pb-4">
      <p className="text-xs text-zinc-500 dark:text-zinc-400">
        Action templates are configured directly on each automation's Action node.
        Configure the operation, target table, source query, and field mapping in the canvas.
      </p>
    </div>
  );
}
